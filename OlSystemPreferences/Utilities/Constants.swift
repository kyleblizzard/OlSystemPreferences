import Cocoa

enum AppConstants {
    static let gridWindowSize = NSSize(width: 668, height: 444)
    static let defaultPaneSize = NSSize(width: 668, height: 500)
    static let animationDuration: TimeInterval = 0.25
    static let iconSize: CGFloat = 48
    static let gridItemSize = NSSize(width: 100, height: 80)
    static let gridSectionInset = NSEdgeInsets(top: 10, left: 20, bottom: 10, right: 20)
    static let gridInteritemSpacing: CGFloat = 10
    static let gridLineSpacing: CGFloat = 10
    static let windowMinSize = NSSize(width: 540, height: 300)
}
